import { VazirFontDirective } from './vazir-font.directive';

describe('VazirFontDirective', () => {
  it('should create an instance', () => {
    const directive = new VazirFontDirective();
    expect(directive).toBeTruthy();
  });
});
